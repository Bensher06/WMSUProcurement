/**
 * Upper bound for `profiles.middle_initial` (middle name or initial).
 * Keep in sync with migration `profiles_middle_initial_len` on `public.profiles`.
 */
export const PROFILE_MIDDLE_INITIAL_MAX_LEN = 120;
